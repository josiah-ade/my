export enum GroupAutomationType {
    WORD = "wordTriggered",
    DATE = "dateTriggered",
  }
  
  export enum AutomationMatchType {
    EXACT = "exact",
    SIMILAR = "similarity",
  }
  
  export enum OperationStatus {
    ACTIVE = "active",
    INACTIVE = "inActive",
  }
  export enum AutomationTemplateMatchType{
    Word="wordTriggered",
    TIME="timeTriggered",
  }
  